import { createReducer } from "@reduxjs/toolkit";

interface IssueReducerState {}
export const initialState = {} as IssueReducerState
export const issueReducer = createReducer(initialState, builder => {})
